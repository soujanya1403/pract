name: Repo Vars and Secrets
on: workflow_dispatch
env:
  mypasswd: ${{ secrets.EMAILPASSWD }} 
jobs:
  jobx:
    runs-on: ubuntu-latest
    steps:
    - name: step1
      run: |
        echo "Application Type os ${{ vars.APP_TYPE }}"
        echo "Owner of The Application ${{ vars.OWNER }}"
        echo "Email Passwd is ${{ env.mypasswd }}"